{
  "name": "whatlog",
  "module": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --compile --outfile dist/wl",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "type": "module",
  "dependencies": {
    "@bloodyowl/boxed": "^3.4.0",
    "temporal-polyfill": "^0.3.2"
  }
}
